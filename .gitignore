# Veil
/bin/
/dist/
/coverage.out
*.db
*.log
.env
node_modules/
web/dist/
coverage*.out
api.test
/scratch/
/veil
.codegraph/
/.worktrees/
/coverage
acme/

# web frontend
web/node_modules/
web/dist/

# TanStack Router file-based route tree (regenerated by the Vite plugin on
# every build; also via `pnpm router:gen`). Not a source artifact.
web/src/routeTree.gen.ts

# TypeScript incremental build cache.
web/tsconfig.tsbuildinfo

# Playwright runtime output is never source.
test/browser/test-results/
test/browser/playwright-report/

# Runtime artifacts written by api package tests (apply history/state fixtures)
internal/api/generated/
.artifacts/

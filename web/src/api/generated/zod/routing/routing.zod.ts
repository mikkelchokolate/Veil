/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Veil Panel API
 * HTTP management API exposed by the Veil Panel (`veil serve`).
 *
 * ## Authentication model
 * Veil is fail-closed for public Panel exposure. A non-loopback `--listen`
 * address requires native TLS, API token authentication (`--auth-token` or
 * `VEIL_API_TOKEN`), user/session authentication, and authenticated metrics.
 * Caddy exposure requires user/session authentication and authenticated
 * metrics even though the Veil listener remains on loopback.
 *
 * An unconfigured local loopback listener exposes only the first-run setup
 * flow needed to create the initial administrator. Setup is unavailable
 * through direct or Caddy exposure.
 *
 * `/api/*` routes accept either `X-Veil-Token`, `Authorization: Bearer`, or
 * a `veil_session` cookie created by `/api/auth/login`. Mutating requests
 * made with a cookie session must also send `X-CSRF-Token`, and only `admin`
 * sessions may mutate management state. The sole viewer mutation exception
 * is `POST /api/auth/locale`, which updates only the authenticated user's own
 * language preference and rejects static API-token callers.
 *
 * Browser sessions survive Panel restarts. Only hashes of the session cookie
 * and CSRF token are persisted. Sessions expire after 30 minutes idle or 24
 * hours absolute and are revoked when a user's password, role, or existence
 * changes.
 *
 * `/healthz`, `/metrics`, and the Panel UI are not treated as proof of
 * authorization. On public listeners `/healthz` is authenticated, `/metrics`
 * follows the separate metrics policy (`--metrics-access` /
 * `VEIL_METRICS_ACCESS`), and the Panel UI renders only the login surface
 * until a valid session exists.
 *
 * Validation and ordinary HTTP failures use a `text/plain` response body.
 * Failures returned by the privileged helper use the structured
 * `PrivilegedErrorEnvelope` JSON schema so operators and generated clients
 * can distinguish policy, availability, validation, and execution failures.
 *
 * OpenAPI spec version: 0.6.3
 */
import * as zod from 'zod';


/**
 * @summary List routing rules
 */
export const GetApiRoutingRulesResponseItem = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})
export const GetApiRoutingRulesResponse = zod.array(GetApiRoutingRulesResponseItem)

/**
 * @summary Create a routing rule
 */
export const postApiRoutingRulesHeaderIdempotencyKeyMax = 128;


export const postApiRoutingRulesHeaderIdempotencyKeyRegExp = new RegExp('^[!-~]+$');


export const PostApiRoutingRulesHeader = zod.object({
  "Idempotency-Key": zod.string().min(1).max(postApiRoutingRulesHeaderIdempotencyKeyMax).regex(postApiRoutingRulesHeaderIdempotencyKeyRegExp).optional().describe('Optional replay key for create, update, and destructive operations. Reuse with a different payload returns 409.')
})

export const PostApiRoutingRulesBody = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})

export const PostApiRoutingRulesResponse = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
}).and(zod.object({
  "success": zod.boolean(),
  "revision": zod.object({
  "desired": zod.int(),
  "applied": zod.int(),
  "state": zod.enum(['synced', 'pending', 'applying', 'failed', 'rolling_back', 'rolled_back', 'degraded', 'recovering', 'untracked'])
}),
  "applyJob": zod.object({
  "id": zod.string(),
  "desiredRevision": zod.int(),
  "baseRevision": zod.int(),
  "status": zod.enum(['pending', 'planning', 'validating', 'applying', 'health_check', 'staged', 'recovery_pending', 'succeeded', 'failed', 'rolling_back', 'rolled_back', 'rollback_failed']),
  "trigger": zod.string(),
  "actorId": zod.string().optional(),
  "createdAt": zod.int(),
  "startedAt": zod.int().optional(),
  "finishedAt": zod.int().optional(),
  "errorCode": zod.string().optional(),
  "errorMessage": zod.string().optional(),
  "operations": zod.array(zod.object({
  "type": zod.string().describe('Runtime operation kind attempted by the job (for example promote_file, reload_service, restart_service, panel-update-install, panel-update-restart).'),
  "target": zod.string().optional(),
  "success": zod.boolean(),
  "detail": zod.string().optional()
})).optional().describe('Concrete runtime changes attempted by this job.'),
  "ownerProcess": zod.string().optional().describe('Lease owner identity of the process that ran the job.'),
  "leaseGeneration": zod.int().optional().describe('Fencing generation of the apply lease held while the job ran.')
}).optional()
}).describe('Apply outcome merged into admin mutation responses. success=false means the change committed (desired revision advanced) but the apply job for that revision did not finish cleanly; inspect applyJob for evidence.'))

/**
 * @summary Read a routing rule
 */



export const GetApiRoutingRulesNameParams = zod.object({
  "name": zod.string().min(1)
})

export const GetApiRoutingRulesNameResponse = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})

/**
 * @summary Update a routing rule
 */



export const PutApiRoutingRulesNameParams = zod.object({
  "name": zod.string().min(1)
})

export const putApiRoutingRulesNameHeaderIdempotencyKeyMax = 128;


export const putApiRoutingRulesNameHeaderIdempotencyKeyRegExp = new RegExp('^[!-~]+$');


export const PutApiRoutingRulesNameHeader = zod.object({
  "Idempotency-Key": zod.string().min(1).max(putApiRoutingRulesNameHeaderIdempotencyKeyMax).regex(putApiRoutingRulesNameHeaderIdempotencyKeyRegExp).optional().describe('Optional replay key for create, update, and destructive operations. Reuse with a different payload returns 409.')
})

export const PutApiRoutingRulesNameBody = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})

export const PutApiRoutingRulesNameResponse = zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
}).and(zod.object({
  "success": zod.boolean(),
  "revision": zod.object({
  "desired": zod.int(),
  "applied": zod.int(),
  "state": zod.enum(['synced', 'pending', 'applying', 'failed', 'rolling_back', 'rolled_back', 'degraded', 'recovering', 'untracked'])
}),
  "applyJob": zod.object({
  "id": zod.string(),
  "desiredRevision": zod.int(),
  "baseRevision": zod.int(),
  "status": zod.enum(['pending', 'planning', 'validating', 'applying', 'health_check', 'staged', 'recovery_pending', 'succeeded', 'failed', 'rolling_back', 'rolled_back', 'rollback_failed']),
  "trigger": zod.string(),
  "actorId": zod.string().optional(),
  "createdAt": zod.int(),
  "startedAt": zod.int().optional(),
  "finishedAt": zod.int().optional(),
  "errorCode": zod.string().optional(),
  "errorMessage": zod.string().optional(),
  "operations": zod.array(zod.object({
  "type": zod.string().describe('Runtime operation kind attempted by the job (for example promote_file, reload_service, restart_service, panel-update-install, panel-update-restart).'),
  "target": zod.string().optional(),
  "success": zod.boolean(),
  "detail": zod.string().optional()
})).optional().describe('Concrete runtime changes attempted by this job.'),
  "ownerProcess": zod.string().optional().describe('Lease owner identity of the process that ran the job.'),
  "leaseGeneration": zod.int().optional().describe('Fencing generation of the apply lease held while the job ran.')
}).optional()
}).describe('Apply outcome merged into admin mutation responses. success=false means the change committed (desired revision advanced) but the apply job for that revision did not finish cleanly; inspect applyJob for evidence.'))

/**
 * @summary Delete a routing rule
 */



export const DeleteApiRoutingRulesNameParams = zod.object({
  "name": zod.string().min(1)
})

export const deleteApiRoutingRulesNameHeaderIdempotencyKeyMax = 128;


export const deleteApiRoutingRulesNameHeaderIdempotencyKeyRegExp = new RegExp('^[!-~]+$');


export const DeleteApiRoutingRulesNameHeader = zod.object({
  "Idempotency-Key": zod.string().min(1).max(deleteApiRoutingRulesNameHeaderIdempotencyKeyMax).regex(deleteApiRoutingRulesNameHeaderIdempotencyKeyRegExp).optional().describe('Optional replay key for create, update, and destructive operations. Reuse with a different payload returns 409.')
})

export const DeleteApiRoutingRulesNameResponse = zod.object({
  "name": zod.string()
}).and(zod.object({
  "success": zod.boolean(),
  "revision": zod.object({
  "desired": zod.int(),
  "applied": zod.int(),
  "state": zod.enum(['synced', 'pending', 'applying', 'failed', 'rolling_back', 'rolled_back', 'degraded', 'recovering', 'untracked'])
}),
  "applyJob": zod.object({
  "id": zod.string(),
  "desiredRevision": zod.int(),
  "baseRevision": zod.int(),
  "status": zod.enum(['pending', 'planning', 'validating', 'applying', 'health_check', 'staged', 'recovery_pending', 'succeeded', 'failed', 'rolling_back', 'rolled_back', 'rollback_failed']),
  "trigger": zod.string(),
  "actorId": zod.string().optional(),
  "createdAt": zod.int(),
  "startedAt": zod.int().optional(),
  "finishedAt": zod.int().optional(),
  "errorCode": zod.string().optional(),
  "errorMessage": zod.string().optional(),
  "operations": zod.array(zod.object({
  "type": zod.string().describe('Runtime operation kind attempted by the job (for example promote_file, reload_service, restart_service, panel-update-install, panel-update-restart).'),
  "target": zod.string().optional(),
  "success": zod.boolean(),
  "detail": zod.string().optional()
})).optional().describe('Concrete runtime changes attempted by this job.'),
  "ownerProcess": zod.string().optional().describe('Lease owner identity of the process that ran the job.'),
  "leaseGeneration": zod.int().optional().describe('Fencing generation of the apply lease held while the job ran.')
}).optional()
}).describe('Apply outcome merged into admin mutation responses. success=false means the change committed (desired revision advanced) but the apply job for that revision did not finish cleanly; inspect applyJob for evidence.'))

/**
 * @summary List routing presets and source material
 */
export const getApiRoutingPresetsResponseSourceFilesItemOnePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const getApiRoutingPresetsResponseSourceFilesItemTwoPinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const getApiRoutingPresetsResponseSourceFilesItemThreePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const getApiRoutingPresetsResponsePresetsItemSourceFilesItemOnePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const getApiRoutingPresetsResponsePresetsItemSourceFilesItemTwoPinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const getApiRoutingPresetsResponsePresetsItemSourceFilesItemThreePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');


export const GetApiRoutingPresetsResponse = zod.object({
  "activePreset": zod.string().optional(),
  "source": zod.object({
  "repository": zod.string().optional(),
  "files": zod.array(zod.union([zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponseSourceFilesItemOnePinnedSha256RegExp).describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}),zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponseSourceFilesItemTwoPinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().describe('Exact expected Sigstore certificate OIDC issuer.')
})]).and(zod.object({
  "name": zod.string(),
  "url": zod.url(),
  "sha256Url": zod.url(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponseSourceFilesItemThreePinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}))).optional()
}),
  "rules": zod.array(zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})),
  "presets": zod.array(zod.object({
  "name": zod.string(),
  "description": zod.string(),
  "source": zod.object({
  "repository": zod.string().optional(),
  "files": zod.array(zod.union([zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponsePresetsItemSourceFilesItemOnePinnedSha256RegExp).describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}),zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponsePresetsItemSourceFilesItemTwoPinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().describe('Exact expected Sigstore certificate OIDC issuer.')
})]).and(zod.object({
  "name": zod.string(),
  "url": zod.url(),
  "sha256Url": zod.url(),
  "pinnedSha256": zod.string().regex(getApiRoutingPresetsResponsePresetsItemSourceFilesItemThreePinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}))).optional()
}),
  "rules": zod.array(zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
}))
})).optional()
})

/**
 * @summary Apply a routing preset
 */



export const PostApiRoutingPresetsNameParams = zod.object({
  "name": zod.string().min(1)
})

export const postApiRoutingPresetsNameHeaderIdempotencyKeyMax = 128;


export const postApiRoutingPresetsNameHeaderIdempotencyKeyRegExp = new RegExp('^[!-~]+$');


export const PostApiRoutingPresetsNameHeader = zod.object({
  "Idempotency-Key": zod.string().min(1).max(postApiRoutingPresetsNameHeaderIdempotencyKeyMax).regex(postApiRoutingPresetsNameHeaderIdempotencyKeyRegExp).optional().describe('Optional replay key for create, update, and destructive operations. Reuse with a different payload returns 409.')
})

export const postApiRoutingPresetsNameResponseOneSourceFilesItemOnePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const postApiRoutingPresetsNameResponseOneSourceFilesItemTwoPinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const postApiRoutingPresetsNameResponseOneSourceFilesItemThreePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemOnePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemTwoPinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');
export const postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemThreePinnedSha256RegExp = new RegExp('^[0-9a-f]{64}$');


export const PostApiRoutingPresetsNameResponse = zod.object({
  "activePreset": zod.string().optional(),
  "source": zod.object({
  "repository": zod.string().optional(),
  "files": zod.array(zod.union([zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOneSourceFilesItemOnePinnedSha256RegExp).describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}),zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOneSourceFilesItemTwoPinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().describe('Exact expected Sigstore certificate OIDC issuer.')
})]).and(zod.object({
  "name": zod.string(),
  "url": zod.url(),
  "sha256Url": zod.url(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOneSourceFilesItemThreePinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}))).optional()
}),
  "rules": zod.array(zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
})),
  "presets": zod.array(zod.object({
  "name": zod.string(),
  "description": zod.string(),
  "source": zod.object({
  "repository": zod.string().optional(),
  "files": zod.array(zod.union([zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemOnePinnedSha256RegExp).describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}),zod.object({
  "name": zod.string().optional(),
  "url": zod.url().optional(),
  "sha256Url": zod.url().optional(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemTwoPinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().describe('Exact expected Sigstore certificate OIDC issuer.')
})]).and(zod.object({
  "name": zod.string(),
  "url": zod.url(),
  "sha256Url": zod.url(),
  "pinnedSha256": zod.string().regex(postApiRoutingPresetsNameResponseOnePresetsItemSourceFilesItemThreePinnedSha256RegExp).optional().describe('Exact digest anchored in the reviewed Veil source for immutable built-in assets.'),
  "signatureUrl": zod.url().optional().describe('Sigstore bundle URL for an updateable external source.'),
  "certificateIdentity": zod.string().optional().describe('Exact expected Sigstore signing certificate identity.'),
  "certificateOidcIssuer": zod.url().optional().describe('Exact expected Sigstore certificate OIDC issuer.')
}))).optional()
}),
  "rules": zod.array(zod.object({
  "name": zod.string(),
  "match": zod.string(),
  "outbound": zod.string(),
  "enabled": zod.boolean()
}))
})).optional()
}).and(zod.object({
  "success": zod.boolean(),
  "revision": zod.object({
  "desired": zod.int(),
  "applied": zod.int(),
  "state": zod.enum(['synced', 'pending', 'applying', 'failed', 'rolling_back', 'rolled_back', 'degraded', 'recovering', 'untracked'])
}),
  "applyJob": zod.object({
  "id": zod.string(),
  "desiredRevision": zod.int(),
  "baseRevision": zod.int(),
  "status": zod.enum(['pending', 'planning', 'validating', 'applying', 'health_check', 'staged', 'recovery_pending', 'succeeded', 'failed', 'rolling_back', 'rolled_back', 'rollback_failed']),
  "trigger": zod.string(),
  "actorId": zod.string().optional(),
  "createdAt": zod.int(),
  "startedAt": zod.int().optional(),
  "finishedAt": zod.int().optional(),
  "errorCode": zod.string().optional(),
  "errorMessage": zod.string().optional(),
  "operations": zod.array(zod.object({
  "type": zod.string().describe('Runtime operation kind attempted by the job (for example promote_file, reload_service, restart_service, panel-update-install, panel-update-restart).'),
  "target": zod.string().optional(),
  "success": zod.boolean(),
  "detail": zod.string().optional()
})).optional().describe('Concrete runtime changes attempted by this job.'),
  "ownerProcess": zod.string().optional().describe('Lease owner identity of the process that ran the job.'),
  "leaseGeneration": zod.int().optional().describe('Fencing generation of the apply lease held while the job ran.')
}).optional()
}).describe('Apply outcome merged into admin mutation responses. success=false means the change committed (desired revision advanced) but the apply job for that revision did not finish cleanly; inspect applyJob for evidence.'))

